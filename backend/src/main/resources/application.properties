# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/payment_gateway
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080

# Logging Configuration
logging.level.com.payment.gateway=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Audit Logging Configuration
app.audit.enabled=true
app.audit.async.enabled=true
app.audit.retention.years=7
app.audit.cleanup.interval.hours=24

# Application Configuration
app.payment.gateway.name=Payment Gateway
app.payment.gateway.version=1.0.0

# Security Configuration (for future use)
# spring.security.user.name=admin
# spring.security.user.password=admin

# Actuator Configuration (for monitoring)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Bank Integration Configuration

# Garanti BBVA Test Environment
app.bank.garanti.test.enabled=true
app.bank.garanti.test.endpoint=http://localhost:8080/api/mock/garanti
app.bank.garanti.test.merchant-id=000000000111111
app.bank.garanti.test.terminal-id=00111111
app.bank.garanti.test.username=PROVAUT
app.bank.garanti.test.password=123456

# Callback URLs
app.bank.callback.base-url=http://localhost:8080/api/v1/payments

# Bank Test Cards Configuration
app.bank.test.cards.enabled=true
app.bank.test.simulation.enabled=true

# Fraud Detection Configuration
app.fraud.enabled=true
app.fraud.logging.enabled=true

# Velocity Check Limits
app.fraud.velocity.card.transactions.per.minute=5
app.fraud.velocity.card.transactions.per.hour=20
app.fraud.velocity.card.transactions.per.day=100
app.fraud.velocity.card.amount.per.hour=10000.00
app.fraud.velocity.card.amount.per.day=50000.00
app.fraud.velocity.ip.transactions.per.minute=10
app.fraud.velocity.ip.transactions.per.hour=50
app.fraud.velocity.customer.transactions.per.hour=30
app.fraud.velocity.merchant.transactions.per.minute=100

# Risk Assessment Thresholds
app.fraud.risk.amount.high.threshold=5000.00
app.fraud.risk.amount.very.high.threshold=10000.00
app.fraud.risk.score.decline.threshold=90.00
app.fraud.risk.score.review.threshold=70.00
app.fraud.risk.score.challenge.threshold=40.00

# Blacklist Configuration
app.fraud.blacklist.auto.cleanup.enabled=true
app.fraud.blacklist.cleanup.interval.hours=24

# 3D Secure Configuration
app.payment.gateway.base-url=http://localhost:8080
app.3dsecure.enabled=true
app.3dsecure.timeout.seconds=300

# Garanti BBVA 3D Secure
app.bank.garanti.3d.enabled=true
app.bank.garanti.3d.endpoint=https://sanalposprov.garanti.com.tr/VPosService/v3/Vpos
app.bank.garanti.test.merchant-id=000000000111111
app.bank.garanti.test.terminal-id=00111111
app.bank.garanti.test.username=PROVAUT
app.bank.garanti.test.password=123456

# İş Bankası 3D Secure  
app.bank.isbank.3d.enabled=true
app.bank.isbank.3d.endpoint=https://spos.isbank.com.tr/servlet/est3Dgate
app.bank.isbank.test.client-id=700655000200
app.bank.isbank.test.username=ISTEST
app.bank.isbank.test.password=ISTEST123

# Yapı Kredi 3D Secure
app.bank.yapikredi.3d.enabled=true
app.bank.yapikredi.3d.endpoint=https://setmpos.ykb.com/3DSWebService/YKBPaymentService
app.bank.yapikredi.test.merchant-id=6706598320
app.bank.yapikredi.test.terminal-id=67005551
app.bank.yapikredi.test.posnet-id=27426
app.bank.yapikredi.test.encryption-key=10,10,10,10,10,10,10,10
